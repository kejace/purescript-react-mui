{
  "scripts": {
    "build": "spago bundle-app -m Test.Main --to dist/bundle.js --then 'browserify dist/bundle.js -o dist/bundle.js'",
    "watch": "spago bundle-app -w -m Test.Main --to dist/bundle.js --then 'browserify dist/bundle.js -o dist/bundle.js'"
  },
  "license": "MIT",
  "dependencies": {
    "@material-ui/core": "^3.9.2",
    "create-react-class": "^15.6.2",
    "purescript": "^0.14.3",
    "react": "^16.5.2",
    "react-dom": "^16.5.2"
  }
}
